# Metro times

A WearOS app to display next train times for the Tyne and Wear Metro

## Installation

1. Download the APK from the releases
2. Enable WiFi debugging on your watch
3. In the terminal run `adb pair <ip address> <code>`
4. Run `adb devices`
5. Run `adb -s <device name> install app-release.apk`
